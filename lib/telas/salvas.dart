import 'package:flutter/material.dart';

class Salvas extends StatefulWidget {
  const Salvas({Key? key}) : super(key: key);

  @override
  State<Salvas> createState() => _SalvasState();
}

class _SalvasState extends State<Salvas> {
  @override
  Widget build(BuildContext context) {
    return Container(); // TODO: implementar a tela de palavras salvas e remover da inicial
  }
}
